# Maestro E2E Test Environment Variables
# 
# SECURITY WARNING: This file shows the required structure but should NEVER
# contain actual credentials. Copy this to .env.test (which is gitignored)
# and fill in with real test account credentials.
#
# Setup Instructions:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Create test accounts in Supabase (see TEST_ACCOUNTS.md)
# 3. Fill in the credentials below
# 4. Never commit .env.test to version control
#
# Usage:
# maestro test .maestro/flows --env .maestro/.env.test

# =============================================================================
# Test Account Credentials
# =============================================================================

# Sponsee Test Account
# Used for testing sponsee-specific features and journeys
TEST_SPONSEE_EMAIL=sponsee1@test.example.com
TEST_SPONSEE_PASSWORD=TestPassword123!
TEST_SPONSEE_FIRST_NAME=Jane
TEST_SPONSEE_LAST_INITIAL=D

# Sponsor Test Account  
# Used for testing sponsor-specific features and journeys
TEST_SPONSOR_EMAIL=sponsor1@test.example.com
TEST_SPONSOR_PASSWORD=TestPassword123!
TEST_SPONSOR_FIRST_NAME=John
TEST_SPONSOR_LAST_INITIAL=S

# Both Roles Test Account
# Used for testing users with both sponsor and sponsee roles
TEST_BOTH_ROLE_EMAIL=both1@test.example.com
TEST_BOTH_ROLE_PASSWORD=TestPassword123!
TEST_BOTH_ROLE_FIRST_NAME=Alex
TEST_BOTH_ROLE_LAST_INITIAL=B

# New User Test Accounts (for signup flows)
# These should NOT exist in the database - they will be created during tests
# Use timestamp-based emails to ensure uniqueness: newuser+{timestamp}@test.example.com
TEST_NEW_USER_EMAIL_PREFIX=newuser
TEST_NEW_USER_DOMAIN=test.example.com
TEST_NEW_USER_PASSWORD=NewUserTest123!

# OAuth Test Accounts (Google/Facebook)
# Only needed if testing OAuth flows
TEST_GOOGLE_EMAIL=
TEST_GOOGLE_PASSWORD=
TEST_FACEBOOK_EMAIL=
TEST_FACEBOOK_PASSWORD=

# =============================================================================
# Supabase Configuration
# =============================================================================

# These should match your project's Supabase settings
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
EXPO_PUBLIC_FACEBOOK_APP_ID=your-facebook-app-id

# =============================================================================
# Test Data Configuration
# =============================================================================

# Test Invite Code (for sponsor-sponsee connection tests)
# This should be a valid invite code created by TEST_SPONSOR_EMAIL account
TEST_INVITE_CODE=TESTCODE123

# Test Relationship IDs
# Populated after relationship creation in setup flows
TEST_SPONSOR_SPONSEE_RELATIONSHIP_ID=

# =============================================================================
# Test Execution Configuration
# =============================================================================

# Device/Simulator Settings
TEST_DEVICE_PLATFORM=ios  # ios, android, or web
TEST_DEVICE_NAME=iPhone 15  # Device name for iOS/Android

# Test Timeouts (milliseconds)
TEST_ASSERTION_TIMEOUT=5000
TEST_FLOW_TIMEOUT=300000

# Debug Mode
TEST_DEBUG_MODE=false
TEST_SCREENSHOT_ON_FAILURE=true

# =============================================================================
# CI/CD Configuration
# =============================================================================

# Set to true when running in CI environment
CI_MODE=false

# Maestro Cloud Configuration (for parallel execution)
MAESTRO_CLOUD_API_KEY=
MAESTRO_CLOUD_WORKSPACE=
